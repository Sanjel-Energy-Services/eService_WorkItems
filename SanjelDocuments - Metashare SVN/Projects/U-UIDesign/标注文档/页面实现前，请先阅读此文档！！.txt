标注统一说明文档：

1、标注分文三大页面：① W-代表只标注页面中的宽度。
                     ② H-代表只标注页面中的高度。
                     ③ fonts-代表对页面中的文字大小、颜色、按钮的大小等一切属性的说明。
2、同色块的样式（H/W/Other）只进行一次性标注。如果再次遇到同色块，但未标注的地方，请参考同色快的标注样式。
                 